{
  "name": "brother-tours-operations-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Brother Tours operations management hub backed by the existing WordPress Operations API.",
  "type": "module",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "npm run dev --workspace=brother-tours-operations-web",
    "build": "npm run build:production",
    "build:production": "npm run preflight:production && npm run build:production --workspace=brother-tours-operations-web && npm run stamp",
    "stamp": "node scripts/stamp.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "start": "node server.mjs",
    "check": "npm run build && npm run verify:build",
    "preflight": "node scripts/preflight.mjs",
    "preflight:production": "node scripts/preflight.mjs .env.production"
  },
  "overrides": {
    "esbuild": "^0.28.2"
  },
  "workspaces": [
    "apps/*"
  ]
}
