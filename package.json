{
  "name": "brother-tours-operations-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Brother Tours operations management hub backed by the existing WordPress Operations API.",
  "type": "module",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "dev": "npm run dev --workspace=brother-tours-operations-web",
    "build": "npm run preflight && npm run build --workspace=brother-tours-operations-web",
    "build:staging": "npm run preflight:staging && npm run build:staging --workspace=brother-tours-operations-web",
    "build:production": "npm run preflight:production && npm run build:production --workspace=brother-tours-operations-web",
    "start": "node server.mjs",
    "check": "npm run preflight && npm run build --workspace=brother-tours-operations-web",
    "preflight": "node scripts/preflight.mjs",
    "preflight:staging": "node scripts/preflight.mjs .env.staging",
    "preflight:production": "node scripts/preflight.mjs .env.production"
  },
  "overrides": {
    "esbuild": "^0.28.2"
  },
  "workspaces": [
    "apps/*"
  ]
}
