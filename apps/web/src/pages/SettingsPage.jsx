import React from 'react';
import { Helmet } from 'react-helmet-async';
import { ExternalLink, KeyRound, Server, ShieldCheck, UserRound } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';
import ThemeMenu from '@/components/shell/ThemeMenu';

export default function SettingsPage(){const {user}=useAuth();return <><Helmet><title>Settings · Brother Tours Operations</title><meta name="robots" content="noindex,nofollow"/></Helmet><div className="space-y-5"><div><h1 className="text-xl font-semibold">Settings</h1><p className="mt-1 text-sm text-muted-foreground">Account and application information. Business settings remain authoritative in WordPress.</p></div><div className="grid gap-4 lg:grid-cols-2"><section className="rounded-xl border border-border bg-card p-5"><div className="flex items-center gap-2"><UserRound className="h-5 w-5 text-primary"/><h2 className="font-semibold">Signed-in account</h2></div><dl className="mt-4 space-y-3 text-sm"><div><dt className="text-xs text-muted-foreground">Name</dt><dd className="mt-1 font-medium">{user?.displayName||user?.name||'—'}</dd></div><div><dt className="text-xs text-muted-foreground">Email</dt><dd className="mt-1 font-medium">{user?.email||'—'}</dd></div><div><dt className="text-xs text-muted-foreground">Roles</dt><dd className="mt-1 font-medium">{(user?.roles||[]).join(', ')||'—'}</dd></div></dl></section><section className="rounded-xl border border-border bg-card p-5"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-primary"/><h2 className="font-semibold">Security</h2></div><div className="mt-4 space-y-3 text-sm text-muted-foreground"><p className="flex items-start gap-2"><KeyRound className="mt-0.5 h-4 w-4 shrink-0"/>Authentication is managed by the Brother Tours Operations API using an HttpOnly server session and runtime-only CSRF token.</p><p>No WordPress password or session credential is stored in browser storage.</p></div></section><section className="rounded-xl border border-border bg-card p-5"><div className="flex items-center gap-2"><Server className="h-5 w-5 text-primary"/><h2 className="font-semibold">Backend</h2></div><p className="mt-4 break-all text-sm text-muted-foreground">{import.meta.env.VITE_BT_API_BASE || 'API base not configured at build time.'}</p><a href="/system-health" className="mt-4 inline-flex items-center gap-2 text-sm font-medium text-primary">Open System Health<ExternalLink className="h-4 w-4"/></a></section><section className="rounded-xl border border-border bg-card p-5"><h2 className="font-semibold">Appearance</h2><p className="mt-2 text-sm text-muted-foreground">Choose light, dark or system appearance.</p><div className="mt-4"><ThemeMenu/></div></section></div><div className="rounded-xl border border-border bg-secondary/20 p-4 text-xs leading-relaxed text-muted-foreground">Operational records, tours, inquiries, Formistic submissions, connections and staff permissions are not stored in this app. WordPress and the existing Brother Tours plugins remain the source of truth.</div></div></>}
